---
id: T1
title: Create TenantForge repo
plan: plan-2026-03-tenantforge-extraction
type: feature
priority: P1
complexity: 20
status: pending
sprint: 1
tags: [setup, infrastructure]
depends_on: []
trello_card_id: null
completed_at: null
---

# Objective

Create a fresh TenantForge repository at `~/projects/kits/tenantforge/`. Set up Turborepo + pnpm workspace mirroring the EdgeStack structure. Copy infrastructure configs: wrangler.toml, tsconfig, ESLint, CI workflows. The repo should build and lint cleanly with no application code yet.

# Files

Source files:
- `~/projects/kits/tenantforge/` (new repo root)
- `turbo.json` (new) -- Turborepo configuration
- `pnpm-workspace.yaml` (new) -- pnpm workspace definition
- `wrangler.toml` (new) -- Cloudflare Workers deployment config
- `tsconfig.json` (new) -- root TypeScript config
- `.eslintrc.js` (new) -- ESLint configuration
- `.github/workflows/` (new) -- CI workflow files

# Implementation Plan

1. `git init` fresh repo at `~/projects/kits/tenantforge/`
2. Set up Turborepo + pnpm workspace mirroring EdgeStack structure
3. Copy and adapt infrastructure configs from Qluo: wrangler.toml, tsconfig, ESLint, CI workflows
4. Verify the empty repo builds and lints successfully

# Acceptance Criteria

- [ ] Fresh git repo exists at `~/projects/kits/tenantforge/`
- [ ] Turborepo + pnpm workspace configured mirroring EdgeStack structure
- [ ] Infrastructure configs copied and adapted: wrangler.toml, tsconfig, ESLint, CI workflows
- [ ] Empty repo builds and lints successfully

# Verification

- `cd ~/projects/kits/tenantforge && pnpm install && pnpm build` -- succeeds
- `pnpm lint` -- no errors
