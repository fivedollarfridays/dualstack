---
id: T11.4
title: Config cleanup — Stripe key prefix and Prometheus dev auth docs
plan: plan-2026-03-security-hardening-r5
type: chore
priority: P2
complexity: 15
status: done
sprint: 11
tags:
- config
- docs
depends_on: []
trello_card_id: '48'
completed_at: '2026-03-08T03:57:38.944346'
---

# Objective

Fix R5 NEW-009 + NEW-010: (1) Document that Prometheus dev config intentionally scrapes without auth, adding a comment explaining why. (2) Evaluate and fix `STRIPE_PUBLISHABLE_KEY` naming in `frontend/.env.example` — if the key is needed client-side, it must use `NEXT_PUBLIC_` prefix; if server-side only, document why.

# Files to Update

| File | Change |
|------|--------|
| `monitoring/prometheus/prometheus.yml` | Add comment documenting intentional lack of auth in dev config |
| `frontend/.env.example` | Fix or document `STRIPE_PUBLISHABLE_KEY` naming — add `NEXT_PUBLIC_` prefix if client-side, or add comment explaining server-side usage |
| `frontend/src/lib/stripe-config.ts` (or equivalent) | Update env var reference if renamed |

# Implementation Plan

1. Check how `STRIPE_PUBLISHABLE_KEY` is actually used:
   - Search frontend codebase for references
   - Determine if it's accessed client-side (needs `NEXT_PUBLIC_`) or server-side only
2. If client-side: rename to `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY` in `.env.example` and all references
3. If server-side only: add a comment in `.env.example` explaining why it doesn't have `NEXT_PUBLIC_`
4. Add comment to `monitoring/prometheus/prometheus.yml` explaining dev auth is intentionally omitted
5. Verify no broken references

# Acceptance Criteria

- [x] `STRIPE_PUBLISHABLE_KEY` naming is correct for its usage context (client vs server)
- [x] Prometheus dev config has comment explaining intentional lack of auth
- [x] No broken env var references after any rename
- [x] All existing tests pass

# Verification

```bash
cd frontend && grep -r "STRIPE_PUBLISHABLE_KEY" --include="*.ts" --include="*.tsx" --include="*.env*"
cd frontend && npx vitest run --reporter=verbose
```