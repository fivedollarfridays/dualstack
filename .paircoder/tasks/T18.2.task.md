---
id: T18.2
title: Register files + WebSocket routers in main.py
plan: plan-2026-03-production-readiness-fixes
type: bugfix
priority: P0
complexity: 20
status: done
sprint: 18
tags:
- routing
depends_on: []
trello_card_id: '84'
completed_at: '2026-03-15T13:42:43.418179'
---

# Objective

Mount the fully-implemented files router (4 endpoints) and WebSocket router (1 endpoint) in the application, and add missing storage configuration fields to the Settings model.

# Files to Update

| File | Change |
|------|--------|
| `backend/app/main.py` | Add files + ws_routes router registration in `_register_routers` |
| `backend/app/core/config.py` | Add 5 storage config fields to `Settings` |

# Implementation Plan

1. Add storage fields to `Settings` in `config.py`: `storage_bucket`, `storage_access_key`, `storage_secret_key`, `storage_endpoint`, `storage_region` (all empty defaults)
2. Add files router registration in `_register_routers` (follow existing try/except ImportError pattern)
3. Add ws_routes router registration at root level
4. Add production startup warning if storage not configured
5. Run file route tests — all 7 should now pass

# Acceptance Criteria

- [x] Files router mounted at `/api/v1/files`
- [x] WebSocket router mounted (WS at `/ws`)
- [x] 5 storage fields added to `Settings` model with empty defaults
- [x] Production startup warns if storage not configured
- [x] All 7 file route tests pass (currently failing with 404)
- [x] Existing tests unaffected

# Verification

```bash
pytest backend/tests/files/test_file_routes.py -v
pytest backend/tests/ -q --tb=short
```