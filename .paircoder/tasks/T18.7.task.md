---
id: T18.7
title: Dependency pinning + npm audit fix + CSP nonce
plan: plan-2026-03-production-readiness-fixes
type: bugfix
priority: P2
complexity: 20
status: done
sprint: 18
tags:
- dependencies
- security
depends_on: []
trello_card_id: '89'
completed_at: '2026-03-15T13:42:54.793212'
---

# Objective

Pin open-ended pip dependencies, fix npm vulnerability, apply CSP nonce to style-src, and add production guard for dev token.

# Files to Update

| File | Change |
|------|--------|
| `backend/requirements.txt` | Pin `prometheus-client`, `resend`, `APScheduler` to exact versions |
| `frontend/` | Run `npm audit fix` |
| `frontend/src/lib/csp.ts` | Change `style-src 'unsafe-inline'` to `style-src 'nonce-${nonce}'` |
| `frontend/src/lib/auth-config.ts` | Add localhost guard for dev token |

# Implementation Plan

1. Check installed pip versions and pin to exact
2. Run `npm audit fix`
3. Write test for CSP nonce in style-src, update `csp.ts`
4. Write test for dev token guard, update `auth-config.ts`
5. Run all tests

# Acceptance Criteria

- [x] All pip deps pinned to exact versions
- [x] `npm audit` shows 0 high/critical vulnerabilities
- [x] CSP `style-src` uses nonce, no `unsafe-inline`
- [x] Dev token not usable from non-localhost origins
- [x] All backend + frontend tests pass

# Verification

```bash
grep '>=' backend/requirements.txt
cd frontend && npm audit 2>&1 | head -5
grep "unsafe-inline" frontend/src/lib/csp.ts
pytest backend/tests/ -q --tb=short
cd frontend && npx jest --no-coverage
```