---
id: T2.4
title: 'Test coverage: handleResponse tests + metrics decorator cleanup'
plan: plan-2026-03-review-round2-fixes
type: chore
priority: P1
complexity: 20
status: pending
sprint: null
tags: [tests]
depends_on: [T2.3]
---

# Objective

Add missing direct tests for `handleResponse` and clean up redundant test decorators.

# Implementation Plan

## 1. handleResponse tests (`frontend/src/lib/api/shared.test.ts`)

Add test suite covering all 3 branches:
- Non-ok response → throws Error with status message
- 204 response → returns undefined
- OK response → parses and returns JSON

## 2. Remove redundant decorators (`backend/tests/core/test_metrics_auth.py`)

Remove all `@pytest.mark.asyncio` decorators. The project uses `asyncio_mode = auto` in pytest.ini, making them redundant. Other new test files correctly omit them.

### Files to modify

- `frontend/src/lib/api/shared.test.ts`
- `backend/tests/core/test_metrics_auth.py`

# Acceptance Criteria

- [ ] handleResponse has direct tests for all 3 branches
- [ ] @pytest.mark.asyncio decorators removed from test_metrics_auth.py
- [ ] All tests pass, 100% coverage on both stacks

# Verification

- `cd frontend && npm run test:coverage`
- `cd backend && pytest tests/core/test_metrics_auth.py -v`
