---
id: T2.5
title: 'Consider items: type safety, middleware hardening, minor cleanups'
plan: plan-2026-03-review-round2-fixes
type: refactor
priority: P2
complexity: 25
status: pending
sprint: null
tags: [quality]
depends_on: []
---

# Objective

Address remaining "Consider" items for type safety and code quality.

# Implementation Plan

## 1. Auth cache typing (`backend/app/core/auth.py`)

Change `_clerk_auth_cache: dict[str, object]` to `_clerk_auth_cache: dict[str, Any]` with `from typing import Any`.

## 2. DB lazy initialization (`frontend/src/db/index.ts`)

Assess disruption of converting to lazy `getDb()` function. If all consumers are few, convert. Otherwise, add `TURSO_AUTH_TOKEN` guard alongside existing URL guard.

## 3. clerk-config tests (`frontend/src/lib/clerk-config.test.ts`)

Replace brittle hex-color assertions with structural tests:
- Verify appearance object has expected keys
- Don't assert specific color values

### Files to modify

- `backend/app/core/auth.py`
- `frontend/src/db/index.ts`
- `frontend/src/lib/clerk-config.test.ts`

# Acceptance Criteria

- [ ] Cache uses `Any` type instead of `object`
- [ ] DB index has TURSO_AUTH_TOKEN guard (or lazy init)
- [ ] clerk-config tests are structural, not brittle
- [ ] All tests pass, 100% coverage

# Verification

- `cd backend && pytest --cov=app tests/ -v`
- `cd frontend && npm run test:coverage`
