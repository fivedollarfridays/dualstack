---
id: T24.2
title: Env cleanup
plan: plan-2026-03-pre-listing-polish
type: chore
priority: P0
complexity: 15
status: pending
sprint: 24
tags: [config]
depends_on: []
trello_card_id: "101"
---

# Objective

Remove stale `frontend/.env.local` (has `pk_test_placeholder`), add missing `NEXT_PUBLIC_WS_URL` to env example, add description for Stripe price ID.

# Files to Update

| File | Change |
|------|--------|
| `frontend/.env.local` | Delete |
| `frontend/.env.example` | Add `NEXT_PUBLIC_WS_URL` with comment, add Stripe price ID description |
| `tests/test_buyer_experience.py` | Add env validation tests |

# Implementation Plan

1. Add failing tests to `tests/test_buyer_experience.py`:
   - `test_no_env_local_in_tree`
   - `test_env_example_has_ws_url`
   - `test_env_example_stripe_has_comment`
2. Delete `frontend/.env.local`
3. Update `frontend/.env.example`:
   - Add `# WebSocket URL — must use wss:// in production` + `NEXT_PUBLIC_WS_URL=ws://localhost:8000/ws`
   - Add `# Stripe Price ID for Pro plan (from Stripe Dashboard > Products)` before Stripe line
4. Run tests

# Acceptance Criteria

- [ ] `frontend/.env.local` does not exist
- [ ] `frontend/.env.example` contains `NEXT_PUBLIC_WS_URL` with description
- [ ] `NEXT_PUBLIC_STRIPE_PRO_PRICE_ID` has a description comment
- [ ] All tests pass

# Verification

```bash
pytest tests/test_buyer_experience.py -v -k "env"
test ! -f frontend/.env.local && echo "OK"
grep "NEXT_PUBLIC_WS_URL" frontend/.env.example
```
