---
id: T24.3
title: Security patches (Next.js + pyasn1)
plan: plan-2026-03-pre-listing-polish
type: chore
priority: P0
complexity: 20
status: pending
sprint: 24
tags: [security]
depends_on: [T24.5]
trello_card_id: "102"
---

# Objective

Bump Next.js 15.5.12 to 15.5.13 (HTTP smuggling + disk cache CVEs), pin pyasn1>=0.6.3 (CVE-2026-30922).

# Files to Update

| File | Change |
|------|--------|
| `frontend/package.json` | `next: 15.5.13`, `eslint-config-next: 15.5.13` |
| `backend/requirements.txt` | Add `pyasn1>=0.6.3` under "Security pins" section |
| `tests/test_security_versions.py` | New — version validation tests |

# Implementation Plan

1. Write `tests/test_security_versions.py` with failing tests:
   - `test_next_version_patched` — parse package.json, assert next >= 15.5.13
   - `test_pyasn1_pinned` — assert pyasn1 in requirements.txt with >= 0.6.3
2. Update `frontend/package.json`: next and eslint-config-next to 15.5.13
3. Add to `backend/requirements.txt`:
   ```
   # Security pins (transitive dependencies)
   pyasn1>=0.6.3
   ```
4. Run `pnpm install` in frontend
5. Run `pip install -r backend/requirements.txt`
6. Run full test suite (backend + frontend)

# Acceptance Criteria

- [ ] `next` is 15.5.13 in package.json
- [ ] `eslint-config-next` is 15.5.13
- [ ] `pyasn1>=0.6.3` pinned in requirements.txt
- [ ] All 1,357 tests still pass

# Verification

```bash
pytest tests/test_security_versions.py -v
cd frontend && pnpm list next
pip show pyasn1 | grep Version
```
