---
id: T25.6
title: Storage secret guard + hard-fail ws:// in production
plan: plan-2026-03-production-readiness-assessment
type: bugfix
priority: P2
complexity: 10
status: pending
sprint: 25
tags: [security]
depends_on: []
trello_card_id: "110"
---

# Objective

Add missing storage secret key validation and enforce wss:// in production frontend.

# Implementation Plan

1. In `storage.py`, add `storage_secret_key` to the guard check alongside `storage_bucket` and `storage_access_key`
2. In `use-websocket.ts`, throw or return early when `NODE_ENV === 'production'` and WS URL starts with `ws://`

# Acceptance Criteria

- [ ] `get_storage_service()` raises StorageError when `storage_secret_key` is empty
- [ ] Frontend WebSocket hook refuses to connect on `ws://` in production (returns disconnected status)
- [ ] Tests cover both scenarios
- [ ] All existing tests pass

# Verification

- `python -m pytest tests/core/test_storage.py --tb=short -q`
- `cd frontend && pnpm test -- --no-coverage`

# Files to Modify

- `backend/app/core/storage.py`
- `backend/tests/core/test_storage.py`
- `frontend/src/hooks/use-websocket.ts`
- `frontend/src/hooks/use-websocket.test.ts`
