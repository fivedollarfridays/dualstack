---
id: T26.1
title: HTTP auth audience enforcement — pass clerk_audience to ClerkConfig
plan: plan-2026-03-security-hardening-r7
type: bugfix
priority: P1
complexity: 15
status: pending
sprint: 26
tags: [security]
depends_on: []
trello_card_id: "113"
---

# Objective

Fix asymmetry where WS auth validates JWT audience but HTTP auth does not.

# Implementation Plan

1. Read `backend/app/core/auth.py:53` — `ClerkConfig(jwks_url=jwks_url)` does not pass audience
2. Check if `fastapi-clerk-auth` `ClerkConfig` supports an `audience` parameter
3. If yes: pass `audience=settings.clerk_audience` when configured
4. If no: replace with direct `pyjwt.decode` call (same pattern as `ws_auth.py:_verify_token`)
5. Add tests verifying audience enforcement on the HTTP path

# Acceptance Criteria

- [ ] HTTP auth validates JWT audience when `CLERK_AUDIENCE` is set
- [ ] HTTP and WS auth paths have symmetric audience enforcement
- [ ] Dev mode (no JWKS) still works unchanged
- [ ] Tests cover: audience pass, audience fail, no audience configured
- [ ] All existing auth tests pass

# Files to Modify

- `backend/app/core/auth.py`
- `backend/tests/core/test_auth.py`
