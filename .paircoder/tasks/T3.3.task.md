---
id: T3.3
title: Add render.yaml for backend deployment
plan: plan-2026-03-plan-2026-03-launch-prep
type: chore
priority: P1
complexity: 10
status: pending
sprint: null
tags: [deployment]
depends_on: []
---

# Objective

README line 174 says "Or deploy to Render using the included `render.yaml` (when added)." Either add the file or remove the reference. Since we have a working Dockerfile, adding a render.yaml is straightforward.

# Implementation Plan

## 1. Create `render.yaml` at project root

Minimal Render Blueprint for the backend using the existing Dockerfile.

## 2. Update README line 174

Remove "(when added)" from the render.yaml reference.

### Files to create/modify

- `render.yaml` (create)
- `README.md` (update line 174)

# Acceptance Criteria

- [ ] `render.yaml` exists at project root with valid Render Blueprint
- [ ] README reference updated

# Verification

- Visual inspection of render.yaml
- `grep -n "render.yaml" README.md`
