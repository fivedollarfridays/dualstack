---
id: T4.5
title: '[React] Update E2E tests and verify full suite'
plan: plan-2026-03-clerk-auth-bypass
type: feature
priority: P1
complexity: 25
status: pending
sprint: null
tags: []
depends_on: []
trello_card_id: "15"
---

# Objective

Update Playwright E2E tests to work with the dev auth bypass. Landing page tests verify content renders. Dashboard tests verify pages are accessible (no redirect since auth is bypassed). Update `playwright.config.ts` port to 3100. Run full verification: unit tests, build, E2E tests.

# Files

- `e2e/landing.spec.ts` -- landing page E2E tests
- `e2e/dashboard.spec.ts` -- dashboard page E2E tests
- `playwright.config.ts` -- Playwright configuration (port update to 3100)

# Implementation Plan

1. Update `playwright.config.ts`:
   - Change `webServer.port` and `baseURL` port to `3100`
2. Update `e2e/landing.spec.ts`:
   - Verify landing page renders expected content (heading, CTA)
   - Verify navigation links work
3. Update `e2e/dashboard.spec.ts`:
   - Verify dashboard pages are accessible without auth redirect
   - Verify items page renders
   - Verify billing page renders
   - Verify layout renders with dev user button
4. Run full verification:
   - `npm run test:coverage` -- unit tests pass (250+ tests, 99%+)
   - `npm run build` -- build succeeds
   - `npx playwright test` -- E2E tests pass (8 tests)

# Acceptance Criteria

- [ ] `playwright.config.ts` uses port 3100
- [ ] Landing page E2E tests verify page content renders correctly
- [ ] Dashboard E2E tests verify pages are accessible without auth redirect
- [ ] `npm run test:coverage` passes with 250+ tests at 99%+ coverage
- [ ] `npm run build` passes without errors
- [ ] `npx playwright test` passes with 8 E2E tests
- [ ] App renders landing page at `localhost:3100` without Clerk keys

# Verification

- `npm run test:coverage` -- 250+ tests, 99%+ coverage
- `npm run build` -- clean build
- `npx playwright test` -- 8 E2E tests pass
- Manual: `npm run dev -- -p 3100` and visit `http://localhost:3100` -- landing page renders
