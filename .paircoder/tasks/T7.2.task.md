---
id: T7.2
title: 'Metrics endpoint production guard: require METRICS_API_KEY in production'
plan: plan-2026-03-security-remediation
type: chore
priority: P0
complexity: 20
status: done
sprint: null
tags:
- security
depends_on: []
trello_card_id: '23'
completed_at: '2026-03-07T14:15:11.391806'
---

# Objective

Add a production startup guard requiring `METRICS_API_KEY` when `ENVIRONMENT=production` (SEC-003). Currently `/metrics` is world-readable by default, exposing endpoint names, timing histograms, and DB pool state.

# Files to Update

| File | Change |
|------|--------|
| `backend/app/main.py` | Add startup guard for `METRICS_API_KEY` in production (same pattern as existing Clerk/Stripe guards) |
| `backend/tests/test_main.py` | Add tests for metrics API key startup guard |

# Implementation Plan

1. Write failing tests:
   - production startup raises `RuntimeError` when `metrics_api_key` is empty
   - development startup does NOT raise when `metrics_api_key` is empty
   - production startup succeeds when `metrics_api_key` is set
2. Implement startup guard in lifespan function after existing guards

# Acceptance Criteria

- [x] Production startup fails with clear error when `METRICS_API_KEY` is empty
- [x] Development startup works without `METRICS_API_KEY`
- [x] Error message is clear and actionable
- [x] All tests pass
- [x] Architecture checks pass

# Verification

```bash
pytest backend/tests/test_main.py -v -k metrics
bpsai-pair arch check backend/app/main.py
```