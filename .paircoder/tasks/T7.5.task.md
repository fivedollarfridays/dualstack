---
id: T7.5
title: 'Validation and cache hardening: METRICS_API_KEY entropy, auth cache TTL, health
  endpoint, Uvicorn config'
plan: plan-2026-03-security-remediation
type: chore
priority: P2
complexity: 35
status: done
sprint: null
tags:
- security
depends_on:
- T7.2
trello_card_id: '26'
completed_at: '2026-03-07T14:27:46.714737'
---

# Objective

Address low-severity findings: add minimum length validation on `METRICS_API_KEY` (SEC-009), add TTL/eviction to `_clerk_auth_cache` (SEC-010), reduce information exposure in `/health` (SEC-011), and add `--proxy-headers` to Uvicorn config (SEC-012).

# Files to Update

| File | Change |
|------|--------|
| `backend/app/core/config.py` | Add `metrics_api_key` validator for minimum length when non-empty |
| `backend/app/core/auth.py` | Document bounded size of `_clerk_auth_cache` |
| `backend/app/health/checks.py` | Remove `version` and `uptime_seconds` from `/health` response |
| `backend/app/health/models.py` | Update `HealthStatus` model |
| `backend/entrypoint.sh` | Add `--proxy-headers` flag to uvicorn command |
| `backend/tests/` | Update affected tests |

# Implementation Plan

1. Write failing test: `METRICS_API_KEY` with < 16 chars raises validation error
2. Add Pydantic field validator on `metrics_api_key`
3. Document `_clerk_auth_cache` bounded by unique JWKS URLs (typically 1)
4. Write failing test: `/health` does not include version or uptime
5. Remove version/uptime from `/health`; keep in `/health/live`
6. Add `--proxy-headers` to entrypoint.sh

# Acceptance Criteria

- [x] `METRICS_API_KEY` rejects values shorter than 16 characters (when non-empty)
- [x] `_clerk_auth_cache` has documented size bound
- [x] `/health` endpoint does not expose version or uptime
- [x] `/health/live` still returns uptime for K8s
- [x] Uvicorn starts with `--proxy-headers`
- [x] All tests pass
- [x] Architecture checks pass

# Verification

```bash
pytest backend/tests/ -v -k "metrics_api or health"
bpsai-pair arch check backend/app/core/config.py
bpsai-pair arch check backend/app/health/checks.py
```