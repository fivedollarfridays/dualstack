---
id: T8.5
title: Request hardening + config cleanup [NEW-008, NEW-009, NEW-010, NEW-011]
plan: plan-2026-03-security-hardening-r2
type: chore
priority: P2
complexity: 25
status: done
sprint: null
tags: []
depends_on: []
trello_card_id: '32'
completed_at: '2026-03-07T15:41:05.790712'
---

# Objective

Close four Low-severity findings: add `X-User-ID` to CORS `allow_headers` in dev mode (NEW-008), add request body size limit (NEW-009), clean up `alembic.ini` credential placeholder (NEW-010), and add rate limit to `/billing/portal` stub (NEW-011).

# Files to Update

| File | Change |
|------|--------|
| `backend/app/main.py` | Conditionally add `X-User-ID` to CORS `allow_headers` in development |
| `backend/app/main.py` | Add `ContentSizeLimitMiddleware` (or equivalent) with reasonable max body size |
| `backend/alembic.ini` | Replace `driver://user:pass@localhost/dbname` with safe placeholder comment |
| `backend/app/billing/routes.py` | Add `@limiter.limit("10/minute")` to `/billing/portal` endpoint |
| `backend/tests/test_main.py` | Add test for body size limit behavior |
| `backend/tests/billing/test_routes.py` | Add test verifying portal rate limit decorator is present |

# Implementation Plan

1. In `main.py` CORS config, conditionally include `X-User-ID` in `allow_headers` when `environment == "development"`
2. Add request body size limit middleware (1MB default) before route processing
3. In `alembic.ini`, replace the `sqlalchemy.url` placeholder with a comment: `# Set dynamically by alembic/env.py — do not put credentials here`
4. Add `@limiter.limit("10/minute")` and `request: Request` param to `billing_portal` route
5. Write tests

# Acceptance Criteria

- [x] CORS `allow_headers` includes `X-User-ID` in development mode
- [x] CORS `allow_headers` does NOT include `X-User-ID` in production mode
- [x] Request body size limit is enforced (returns 413 for oversized payloads)
- [x] `alembic.ini` does not contain `user:pass` placeholder
- [x] `/billing/portal` has a rate limit decorator
- [x] All tests pass
- [x] Architecture checks pass

# Verification

```bash
pytest tests/test_main.py tests/billing/test_routes.py -v
bpsai-pair arch check backend/app/main.py
grep -c "user:pass" backend/alembic.ini  # Should be 0
```